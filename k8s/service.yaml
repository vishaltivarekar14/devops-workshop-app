apiVersion: v1
kind: Service
metadata:
  name: devops-workshop-app
  labels:
    app: devops-workshop-app
spec:
  type: ClusterIP
  selector:
    app: devops-workshop-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
